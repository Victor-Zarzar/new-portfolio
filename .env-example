NODE_ENV=production
# Options: "development" | "production"
# Controls runtime behavior, optimizations, and feature flags

SMTP_EMAIL=your-email@gmail.com
# Your email address used to send contact form messages
# For Gmail: Use your full Gmail address (e.g., yourname@gmail.com)

SMTP_PASSWORD=your-app-password-here
# App-specific password for SMTP authentication
# For Gmail: Generate at https://myaccount.google.com/apppasswords
# Note: Regular Gmail password won't work - you MUST use an App Password

NEXT_PUBLIC_DISABLE_DEVTOOLS=true
# Disables React DevTools in production builds
# Set to "false" during development if you need DevTools

NEXT_PUBLIC_WEBSITE_URL=http://localhost:3000
# The base URL of your website (used for canonical URLs, Open Graph, etc.)
# Production example: https://yourwebsite.com
# Local development: http://localhost:3000

NEXT_PUBLIC_SENTRY_DSN=https://examplePublicKey@o0000000.ingest.sentry.io/0000000
# Your Sentry DSN (Data Source Name)
# Get this from: Sentry Project Settings > Client Keys (DSN)
# Format: https://[PUBLIC_KEY]@[ORGANIZATION].ingest.sentry.io/[PROJECT_ID]

SENTRY_AUTH_TOKEN=sntrys_exampleAuthToken1234567890abcdef
# Authentication token for Sentry CLI operations
# Generate at: Sentry Settings > Account > Auth Tokens
# Required permissions: project:releases, org:read

SENTRY_RELEASE=my-portfolio@1.0.0
# Unique identifier for this release (used for tracking errors by version)
# Format: projectname@version (e.g., my-website@1.0.0, my-app@2.3.1)

SENTRY_ORG=your-organization-slug
# Your Sentry organization slug
# Find in: Sentry Settings > Organization Settings > General

SENTRY_PROJECT=your-project-name
# Your Sentry project name/slug
# Find in: Sentry Project Settings > General
